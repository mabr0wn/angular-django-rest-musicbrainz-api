from django.contrib import admin

from .models import * # import all the models.

# next we will add the register models, but that is not good programming skills to write 300 lines for admin.register.model
# would be better to figure out a solution.

